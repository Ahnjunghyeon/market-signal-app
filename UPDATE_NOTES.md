# NEWB 업데이트 노트

## v9.7

### Changed
- 기존 수정사항을 유지한 상태에서 설정 버튼 정렬을 보정했습니다.
- watch/news 섹션 배경 경계감을 줄이고 카드/버튼 모션이 잘리지 않도록 보강했습니다.
- stock-add-card 계열의 부드러운 모션을 카드/버튼 전반에 확장했습니다.
- AI 추천 카드의 웹/모바일 배치를 재정리했습니다.
- 자동완성 입력 반응성과 추천 리스트 디자인을 개선했습니다.

### Fixed
- 마우스 휠 세로 스크롤이 막히는 현상을 보정했습니다.
- 알림 메뉴에서 기사 카드 대신 문구/점수 중심 리스트로 표시되도록 수정했습니다.
- 확대 모션 중 텍스트가 흐려지는 현상을 완화했습니다.

### QA
- npm install 완료
- npm run typecheck 통과
- npm run build 통과


---

## v9.6

### Changed
- 모바일 빠른메뉴가 오른쪽으로 밀리는 문제를 보정했습니다.
- 홈/내 주식 등 설정 외 화면의 배경 경계감을 줄이고 카드/버튼 모션이 잘리지 않도록 조정했습니다.
- 뉴스 카드 북마크 버튼을 최우측 상단 모서리 연결형으로 재고정했습니다.
- 요약 카드와 뉴스 카드의 hover/touch 모션을 더 부드럽게 통일했습니다.

### Fixed
- 마우스 휠 세로 스크롤이 막히지 않도록 전역 overflow 설정을 보강했습니다.
- 모바일 뉴스 카드에서 점수/태그/라벨 영역이 좁은 폭에서 겹치거나 잘리는 현상을 완화했습니다.
- 슬라이드 메뉴 닫힘 모션을 자연스럽게 보강했습니다.

### QA
- npm install 확인
- npm run typecheck 확인
- npm run build 확인


---

## v9.3

### Changed
- 전체 카드/버튼 hover·touch 모션을 부드러운 스르륵 방식으로 통일했습니다.
- 모바일 상단 헤더에 블러 배경을 추가해 스크롤 중 텍스트가 섞이지 않도록 개선했습니다.
- 모바일 슬라이드 메뉴 닫기 버튼을 패널 외곽 우측에 자연스럽게 연결했습니다.
- 요약 카드 hover 시 상단이 잘리지 않도록 여백과 그림자를 조정했습니다.
- 뉴스 카드 +N 태그 펼침 시 카드가 아래로 자연스럽게 확장되도록 개선했습니다.
- 관심 종목 카드 별표가 내용 흐름에 영향을 주지 않도록 absolute 배치했습니다.
- 라이트/무신사 블랙 테마의 카드 그림자와 그라데이션 경계감을 완화했습니다.

### QA
- npm install 완료
- npm run typecheck 통과
- npm run build 통과


## v9.1 - Card Corner Action & Responsive Chips

### Changed
- 뉴스 카드 북마크 버튼을 카드 최우측 상단 모서리와 연결된 탭 위치로 보정했습니다.
- 반응형 카드에서 태그가 잘리지 않도록 화면 폭에 따라 표시 개수를 자동 조정했습니다.
- 본문과 점수 사이 분단선은 1개만 유지되도록 정리했습니다.

### QA
- npm install 완료
- npm run typecheck 통과
- npm run build 통과

---


## v9.0

### Changed
- 저장 버튼을 카드 우측 최상단 연결형 탭 위치로 보정했습니다.
- 반응형 카드에서 태그 문구가 잘리지 않도록 칩 표시 방식을 수정했습니다.
- 기사 본문과 점수 사이 분단선을 하나만 남기도록 정리했습니다.
- 모바일/웹 카드 레이아웃의 점수 영역 간격을 보정했습니다.

### QA
- npm install 완료
- npm run typecheck 통과
- npm run build 통과

---

---

## v9.0 - Responsive Card Final Polish

### Changed
- 반응형 뉴스 카드에서 저장 버튼을 카드 우측 상단에 연결된 탭 형태로 재배치했습니다.
- 점수 영역을 분단선과 카드 우측 끝 사이의 중앙에 오도록 보정했습니다.
- 태그/관심 테마 칩이 한 줄 안에서 보이고, 초과분은 +N 버튼으로 접히도록 조정했습니다.
- 모바일 요약 카드 4개가 가로 드래그로 모두 확인되도록 보강했습니다.
- 카드 hover/touch 모션을 더 부드럽게 유지하면서 글자 흐림을 완화했습니다.

### QA
- npm install 확인
- npm run typecheck 확인
- npm run build 확인

---

## v8.8 - Responsive Action Card Refinement

### Changed
- 뉴스 카드의 기사 바로가기 버튼을 제거하고 카드 전체 클릭으로 원문 기사 이동을 통일했습니다.
- 저장 버튼을 카드 최우측 상단에 단독 배치하고 카드 표면과 이어지는 글래스 버튼 톤으로 조정했습니다.
- 카드 본문과 점수 영역 사이에 자연스러운 분단선을 추가해 가독성을 높였습니다.
- 중립/긍정/부정 상태 정보를 TOP/국가/출처 칩과 같은 상단 라벨 계층으로 이동했습니다.
- 웹 간략히/자세히 카드와 모바일 카드의 점수/태그 겹침을 보정했습니다.
- 모바일 종합점수 카드 영역을 가로 드래그로 끝까지 확인할 수 있도록 보강했습니다.
- 슬라이드 메뉴 등장 모션을 slide + fade 방식으로 더 천천히 조정했습니다.

### Fixed
- 카드 hover/touch 시 확대가 갑작스럽고 글자가 흐려지는 느낌을 완화했습니다.
- 로컬 설정 초기화 클릭 시 관심 종목/관심 테마/알림 기준/AI 추천 기준/북마크 상태가 실제 화면에서도 즉시 초기화되도록 수정했습니다.
- 모바일 카드에서 태그와 점수 영역이 겹치거나 가려지는 현상을 완화했습니다.

### QA
- `npm install` 완료
- `npm run typecheck` 통과
- `npm run build` 통과

## v8.7 - Responsive Card Polish

### Added
- 뉴스 카드 태그 `+N` 더보기 / `−` 줄이기 기능을 카드 내부에 추가했습니다.
- 뉴스 카드 우측 상단에 저장 버튼과 기사 바로가기 버튼을 하나의 액션 그룹으로 배치했습니다.
- 모바일 요약 카드 영역을 가로 드래그/스크롤로 끝까지 확인할 수 있도록 보강했습니다.

### Changed
- 반응형 뉴스 카드의 버튼 위치를 우측 상단으로 재정렬했습니다.
- 웹 간략히/자세히 카드 레이아웃을 안정적으로 복구했습니다.
- 카드 hover/touch 모션을 `0.5초` 기반의 부드러운 스르륵 확대 느낌으로 조정했습니다.
- 카드 확대 시 흐려지는 느낌을 줄이기 위해 blur/filter 계열 효과를 제거했습니다.
- 홈 필터 버튼과 간략히/새로고침 버튼을 더 부드러운 블럭형 pill 디자인으로 조정했습니다.
- 무신사 블랙 테마의 상태 카드/필터 버튼/종목 추가 카드 색상을 어두운 퍼플 블랙 톤으로 보정했습니다.

### Fixed
- 모바일 카드에서 태그가 여러 줄로 밀리며 카드가 과도하게 커지는 문제를 완화했습니다.
- 일부 카드 hover 시 갑자기 커지는 느낌을 완화했습니다.
- 간략히 카드에서 레이아웃이 좁게 무너지는 문제를 보정했습니다.

### QA
- `npm install` 완료
- `npm run typecheck` 통과
- `npm run build` 통과

---

## v8.6 - Soft Interaction Card

### Added
- 빠른모음 보기/안보기 전환에 부드러운 전환감을 추가했습니다.
- 관심 칩이 3줄 이상 길어질 경우 더보기/줄이기 기능을 추가했습니다.

### Changed
- 뉴스 카드 클릭 시 원문 기사로 바로 이동하도록 동작을 변경했습니다.
- 기사보기 버튼을 제거하고 카드 전체 클릭 UX로 단순화했습니다.
- 저장 버튼을 우측 상단으로 재배치했습니다.
- 카드 hover/touch 모션을 0.5초 기반으로 조정했습니다.
- 무신사 블랙 슬라이드/상태카드/설정카드 색상을 보정했습니다.

### Fixed
- 자동완성 항목 선택 후 자동완성 레이어가 닫히지 않는 문제를 수정했습니다.
- 확대 시 흐려지는 느낌을 완화했습니다.

### QA
- `npm install` 완료
- `npm run typecheck` 통과
- `npm run build` 통과

---

## v8.5 - Soft Card Recovery

### Changed
- 뉴스 카드 디자인을 복구했습니다.
- 저장/기사보기 버튼 위치를 재정렬했습니다.
- 모바일 기사보기 버튼 표시를 보강했습니다.
- 무신사 블랙 연동 카드 색상을 보정했습니다.
- 모바일 슬라이드 배경/애니메이션을 보정했습니다.
- 설정 화면 중첩 카드감을 완화했습니다.

### QA
- `npm install` 완료
- `npm run typecheck` 통과
- `npm run build` 통과

---

## v8.4 - Soft Premium Card Motion

### Changed
- 카드 hover 확대를 더 천천히 처리했습니다.
- hover 시 흐려지는 느낌을 제거했습니다.
- 뉴스 카드 저장/기사보기 버튼을 우측 상단으로 정렬했습니다.
- 뉴스 카드 입체 블럭형 그라데이션을 보강했습니다.
- 무신사 블랙 테마의 회색 잔여 UI를 보정했습니다.
- AI 추천 설명/새로고침/추천점수 카드 톤을 정리했습니다.
- 홈 필터/간략히/새로고침 버튼 디자인을 개선했습니다.
- 뉴스 상세 모달 톤을 보정했습니다.

---

## v8.3 - Premium Depth Detail

### Changed
- 전체 카드 hover/touch 미세 확대 모션을 강화했습니다.
- 뉴스/종목/AI 추천 카드 입체감을 추가했습니다.
- 무신사 블랙 테마 회색 버튼/카드 색상을 보정했습니다.
- 뉴스 상세 모달을 프리미엄 스타일로 리디자인했습니다.
- 상세 점수/메트릭/AI 요약 영역을 재구성했습니다.

### QA
- `npm run typecheck` 통과
- `npm run build` 통과

---

## v8.2 - Premium Motion AI Layout

### Added
- 전체 카드 hover/press 모션을 추가했습니다.
- 관심종목/관심테마 input hover/focus 모션을 추가했습니다.
- 메뉴 이동/초기 진입 애니메이션을 추가했습니다.

### Changed
- AI 추천 카드 웹/모바일 레이아웃을 재정비했습니다.
- 상업적 이용 가능한 CSS 기반 모션으로 구성했습니다.

---

## v8.1 - Motion Theme Refresh

### Added
- 알림/새로고침 버튼 press 모션을 추가했습니다.
- 라이트 퍼플 / 무신사 블랙 테마 전환을 추가했습니다.
- 모바일 pull-to-refresh 게이지형 로딩을 추가했습니다.

### Changed
- 웹 AI 추천 새로고침 버튼을 모바일 스타일과 통일했습니다.
- 데이터 동기화/새로고침 블러 로딩을 개선했습니다.
- 최소 1초 로딩 연출을 적용했습니다.

### QA
- `npm run typecheck` 통과
- `npm run build` 통과

---

## v8.0 - AI Recommend Refresh

### Added
- 웹 화면 오른쪽 하단 고정 새로고침 버튼을 추가했습니다.
- AI 추천 점수 슬라이더 조작을 보강했습니다.
- 모바일 AI 추천 설명 문구 기본 숨김 + 설명 버튼을 추가했습니다.

### Changed
- `AI 종목` 메뉴명을 `AI 추천`으로 변경했습니다.
- 실시간 추천 새로고침 버튼 디자인/hover를 개선했습니다.
- AI 추천 종목 미노출 문제를 보완했습니다.

### QA
- `npm run typecheck` 통과
- `npm run build` 통과

---

## v7.9 - AI Stock Polish

### Added
- AI 추천 점수 기준 슬라이더를 추가했습니다.
- 실시간 추천 새로고침을 추가했습니다.
- AI 추천 카드 2줄 근거 요약을 추가했습니다.
- 카드 클릭 / `+추가` 버튼 동작을 분리했습니다.

### Fixed
- AI 종목 중복 추천을 제거했습니다.
- 내 주식 관련 뉴스 빈 화면을 보완했습니다.
- 홈 관심종목 요약의 종목추가 버튼이 유지되도록 수정했습니다.

---

## v7.8 - Settings Input AI Stocks

### Added
- 검색 메뉴를 AI 종목 메뉴로 교체했습니다.
- 뉴스 기반 AI 추천 종목 5개 화면을 추가했습니다.

### Fixed
- 홈 관심 종목 카드 별표 클릭 시 관심 제거 기능을 추가했습니다.
- 관심 종목/관심 테마 input 포커스 풀림을 보강했습니다.
- 설정 자동완성 레이어 가림 문제를 수정했습니다.
- 알림 중요도 드래그 조작성을 보강했습니다.
- 설정 > 알림 설정 메뉴를 제거했습니다.
- 입력창과 추천 검색어 간격을 확대했습니다.

---

## v7.7 - Card Input Polish

### Fixed
- input 한 글자 입력 후 포커스 풀림을 보강했습니다.
- 홈 관심종목 미등록 시 임시 종목 카드를 숨겼습니다.
- 알림 메뉴 뉴스 클릭 시 원문 기사로 바로 열리도록 수정했습니다.
- 모바일 관심종목 칩 상세 설명을 숨겼습니다.
- 모바일 뉴스 카드 태그 3개까지만 표시 + 나머지 `+N` 처리했습니다.
- 뉴스 시간 색상 가독성을 개선했습니다.

---

## v7.6 - Streamlined Feed

### Changed
- 설정 화면 종목 입력창을 제거했습니다.
- AI 스캐너/뉴스 메뉴를 제거했습니다.
- 홈 더보기를 제거했습니다.
- 웹 홈 새로고침 버튼을 추가했습니다.
- 모바일 간략히 기능을 제거했습니다.
- 모바일 뉴스 상단 액션을 숨겼습니다.

### Fixed
- 내 주식/검색 입력창 포커스 풀림을 보강했습니다.

---

## v7.5 - Alert Scroll UX

### Added
- 알림 클릭 시 홈의 실제 뉴스 카드 위치로 이동하는 기능을 추가했습니다.
- 이동한 뉴스 카드 하이라이트 표시를 추가했습니다.
- 모바일 헤더를 스크롤 방향에 따라 숨김/표시하도록 수정했습니다.
- 웹/앱 세로 스크롤 + 마우스 드래그 스크롤을 보강했습니다.

### Changed
- 모바일 알림 버튼을 새로고침 왼쪽에 배치했습니다.

---

## v7.4 - Existing Bugfix

### Added
- 알림 모달을 추가했습니다.
- 알림 클릭 시 해당 뉴스 상세로 이동하도록 했습니다.
- 설정 > 일반 3개 메뉴 동작을 연결했습니다.

### Fixed
- 관심 테마 자동완성 상시 노출 오류를 수정했습니다.
- 자동완성 최대 5개 표시 + 내부 스크롤을 적용했습니다.
- 알림 기준 영역 가림 문제를 완화했습니다.
- 뉴스브리핑을 `NEWB`로 변경했습니다.
- 우측 `뉴브` 배지를 제거했습니다.

---

## v7.3 - Priority Bugfix

### Fixed
- 검색창 한 글자 입력 후 포커스 풀림을 수정했습니다.
- 종목 검색 API debounce를 적용했습니다.
- `/api/news` 동일 요청 중복 호출을 차단했습니다.
- 진행 중 요청 AbortController 처리를 보강했습니다.
- 늦게 도착한 이전 응답 무시 처리를 추가했습니다.
- React duplicate key 경고 방지를 보강했습니다.

---

## v7.2 - Request Keyfix

### Fixed
- `/api/news` 반복 호출 문제를 수정했습니다.
- 관심 종목/테마 변경 시 자동 무한 재호출을 방지했습니다.
- 진행 중 요청을 AbortController로 취소하도록 처리했습니다.
- React duplicate key 에러 수정을 보강했습니다.

---

## v7.1 - Logo Keyfix

### Changed
- `Market Signal`을 `뉴스브리핑`, 축약명을 `뉴브`로 변경했습니다.
- 언론사 첫 글자 박스를 원문 도메인 favicon 로고 자동 표시 방식으로 변경했습니다.
- 로고 실패 시 기존 첫 글자 fallback을 유지했습니다.

### Fixed
- 중복 태그를 제거했습니다.
- React duplicate key console error를 수정했습니다.

---

## v7.0 - Purple Commerce Refactor

### Changed
- Purple Commerce Re-Architecture를 적용했습니다.
- 좌측 사이드바 / 중앙 콘텐츠 / 우측 필터 패널 구조로 재편했습니다.
- 모바일 하단 탭바 앱형 구조를 적용했습니다.
- 홈 / 내 주식 / AI 스캐너 / 뉴스 / 검색 / 알림 / 설정 뷰를 분리했습니다.
- 뉴스 상세 시트를 추가했습니다.
- 밝은 배경 + 어두운 퍼플 카드 UI를 적용했습니다.
- 기존 뉴스 API, 종목 API, 관심 설정 기능을 유지했습니다.

---

## v6.x 이전 주요 변경 요약

- 보라/연보라/핑크 계열 전체 테마를 적용했습니다.
- 무신사/지그재그/에이블리 참고형 카드 UI를 적용했습니다.
- 뉴스 카드/설정/필터/상태 UI를 전체 리디자인했습니다.
- PWA/모바일 반응형 UI를 지속 개선했습니다.
- `/api/stocks`, `/api/news`, MY STOCK FEED, 관심 설정 기능을 순차적으로 개선했습니다.
---

## v9.2

### Added
- 모바일 슬라이드 메뉴 닫힘 애니메이션을 추가했습니다.

### Changed
- 뉴스 카드의 태그 더보기 상태에서 카드가 아래로 자연스럽게 확장되도록 조정했습니다.
- 요약 카드 hover 시 상단이 잘리지 않도록 컨테이너 여백과 overflow를 보정했습니다.
- 반응형 뉴스 카드의 점수/본문 배치를 작은 화면에서도 읽기 좋게 재정리했습니다.
- 관심 종목 카드의 별표가 내용 레이아웃에 영향을 주지 않도록 좌측 상단 absolute 배치로 수정했습니다.
- 모바일 요약 카드 영역의 배경 그라데이션/그림자 분리감을 완화했습니다.

### Fixed
- +N 태그 펼침 시 점수 영역과 겹치던 문제를 수정했습니다.
- hover 확대 시 카드 상단이 컨테이너에 잘리던 문제를 완화했습니다.
- 모바일 슬라이드 메뉴 닫기 버튼 클릭 시 즉시 사라져 어색하던 문제를 수정했습니다.

### QA
- npm install 확인
- npm run typecheck 확인
- npm run build 확인

---

## v9.4

### Changed
- 뉴스 카드 북마크 버튼을 카드 최우측 상단 모서리와 이어지는 탭형 디자인으로 재배치했습니다.
- 반응형 뉴스 카드에서 태그 텍스트가 잘리지 않도록 태그 폭/표시 방식을 보정했습니다.
- 뉴스 본문과 점수 사이 분단선이 중복되지 않도록 1개만 유지했습니다.
- 뉴스 더 보기 버튼을 중앙 컴팩트 pill 형태로 정리했습니다.
- 작은 화면 뉴스 카드의 여백과 점수 영역을 더 자연스럽게 보정했습니다.

### QA
- npm install 완료
- npm run typecheck 통과
- npm run build 통과

## v9.5

### Changed
- 사이드 메뉴 아이콘/텍스트 정렬을 한 줄 기준으로 보정했습니다.
- 웹/모바일 뉴스 카드 간략형 레이아웃을 안정화했습니다.
- 국가 필터, 간략히, 새로고침 버튼 주변의 경계감과 hover 잘림을 완화했습니다.
- 설정 화면의 아이콘/텍스트/상태값 정렬을 중앙 기준으로 보정했습니다.
- 하단 모바일 내비게이션이 화면 밖으로 밀리지 않도록 보정했습니다.
- 관심 종목 요약 영역과 종목 추가 카드 여백/그림자/모션을 개선했습니다.

### QA
- npm install
- npm run typecheck
- npm run build
## v10.3.8-hotfix-card-scale-mobile-alert

- 반응형 홈 인사 영역의 중복 알림 버튼을 숨김 처리했습니다.
- 버튼류는 건드리지 않고 뉴스/종목/요약/AI 카드 계열의 hover/active 확대(scale) 모션만 제거했습니다.
- 카드 hover는 translateY 기반의 살짝 떠오르는 느낌만 유지했습니다.

## v10.3.8-hotfix.2

- 카드 계열에만 hover/active 모션 속도를 느리게 조정했습니다.
- `transform`, `box-shadow`, `border-color`, `background-color` transition을 `.46s`로 통일했습니다.
- 버튼 계열은 수정하지 않았습니다.
- 기존 카드 scale 제거 정책은 유지했습니다.

## v10.3.8-hotfix-responsive-card-scroll

- 반응형 summary 카드 크기를 자연스럽게 축소했습니다.
- 반응형 summary 영역에 보이지 않는 가로 터치 스크롤을 추가했습니다.
- 관심 종목 요약 카드 영역에 보이지 않는 가로 터치 스크롤을 추가했습니다.
- content-canvas 관련 롤백 상태는 유지하고 이번 요청 범위만 수정했습니다.


## v10.3.8-hotfix.4 - Responsive Card Visibility

- 반응형 summary 카드 영역을 가로 터치 스크롤 구조로 보강했습니다.
- 반응형 관심종목 요약 카드 영역을 가로 터치 스크롤 구조로 보강했습니다.
- 카드가 우측에서 부자연스럽게 가려지는 느낌을 줄이도록 모바일 카드 폭/간격/우측 여백을 조정했습니다.
- 모바일에서 하단 absolute/fixed 새로고침 버튼을 숨김 처리했습니다.
- content-canvas 관련 배경/패딩은 수정하지 않았습니다.

## v10.3.8-hotfix.5
- 반응형 summary 카드 영역에 마우스 좌우 드래그 스크롤 지원을 추가했습니다.
- 반응형 관심종목 요약 카드 영역에 마우스 좌우 드래그 스크롤 지원을 추가했습니다.
- 터치 스크롤은 기존 동작을 유지하고, 데스크톱 마우스 환경에서만 드래그 보조 로직이 동작하도록 분리했습니다.
- 버튼/카드 디자인과 기존 content-canvas 스타일은 건드리지 않았습니다.


## v10.3.8-hotfix.6

- 반응형 슬라이드 메뉴 닫힘 동작을 천천히 fade-out + slide-out 방식으로 보정했습니다.
- 닫힘 애니메이션이 끝나기 전에 drawer가 언마운트되지 않도록 닫힘 대기 시간을 조정했습니다.
- `drawer-close` 버튼을 `mobile-drawer` 우측 모서리에 이어 붙고 반쯤 튀어나온 형태로 재배치했습니다.
- 기존 카드/버튼/가로 드래그 스크롤 수정사항은 유지했습니다.

## v10.3.8-hotfix.7

- 반응형 `mobile-drawer` 닫기 버튼 디자인만 수정했습니다.
- 닫기 버튼을 drawer 우측으로 더 빼고, 원형에서 라운드 사각형 형태로 변경했습니다.
- 버튼 배경을 drawer 배경색과 맞춰 내부 경계가 끊겨 보이지 않도록 했습니다.
- drawer 바깥으로 튀어나온 영역만 외곽선/그림자가 보이도록 `border-left: 0` 구조로 정리했습니다.

## v10.3.8-hotfix.8
- 모바일 drawer 닫기 버튼을 `mobile-drawer`에서 반쯤 튀어나온 손잡이 형태로 재배치했습니다.
- 닫기 버튼을 drawer 중단 아래쪽으로 내려 어색한 상단 경계 노출을 줄였습니다.
- drawer 닫힘 시 fade-out + slide-out 애니메이션을 다시 보강했습니다.
- 기존 카드/버튼/가로 드래그 스크롤 수정은 유지했습니다.

## v10.3.8-hotfix.9 - Drawer close handle alignment

- `drawer-close` 버튼을 NEWB 브랜드 라인과 같은 높이로 올렸습니다.
- `drawer-close` 버튼 배경을 `mobile-drawer`와 동일한 색상으로 맞췄습니다.
- `mobile-drawer` 우측 마스킹 레이어를 추가해 버튼과 drawer가 겹치는 부분은 drawer가 덮어 자연스럽게 보이도록 수정했습니다.
- drawer 닫기 버튼 클릭/슬라이드 닫힘 시 fade-out + slide-out 모션이 천천히 진행되도록 닫힘 애니메이션과 대기 시간을 조정했습니다.


## v10.3.8-hotfix.10 - Drawer visual inspection fix

- 슬라이드 drawer-close 손잡이 디자인을 다시 점검하고 재정리했습니다.
- 닫기 버튼이 drawer 뒤로 들어가면서 바깥으로만 반쯤 보이도록 z-index 구조를 조정했습니다.
- 기존 전체 높이 마스킹 pseudo 요소를 제거해 우측 경계가 이상하게 보이던 문제를 완화했습니다.
- 슬라이드 닫힘 모션을 fade-out + slide-out으로 유지하되 더 자연스러운 속도로 조정했습니다.


## v10.3.8-hotfix.11 - Drawer close natural polish

- 모바일 슬라이드 닫기 버튼을 과한 손잡이 형태에서 내부 플로팅 라운드 사각형 버튼으로 정리했습니다.
- drawer 패널 배경/경계/그림자를 단일 패널 기준으로 재정리해 우측 경계가 어색하게 겹치지 않도록 수정했습니다.
- drawer 닫힘 대기 시간을 580ms로 조정하고 fade-out + slide-out 애니메이션을 자연스럽게 맞췄습니다.
- 슬라이드 관련 CSS와 닫힘 타이밍만 수정했습니다.

## v10.3.8-hotfix.12 - Summary hover / AI recommendation card polish

- `summary-grid` 내부 카드 hover 시 그림자가 부모 경계를 드러내지 않도록 hover shadow를 약하게 줄였습니다.
- AI 추천 카드 상단의 TOP 배지와 점수를 같은 배지 스타일로 정리하고 `100/100` 형식으로 표시했습니다.
- AI 추천 카드의 종목명과 종목코드를 가로 배치로 정리했습니다.
- `뉴스 키워드와 업종 흐름에서 감지` 문구가 노출되지 않도록 추천 사유 문구와 카드 레이아웃을 정리했습니다.
- AI 추천 카드의 `+ 추가` 버튼을 우측 탭형 버튼으로 재배치해 카드 높이를 더 감각적으로 줄였습니다.

## v10.3.8-hotfix.13 - AI 추천/알림/하단 네비 polish

- AI 추천 새로고침 버튼을 더 고급스럽고 자연스러운 pill 스타일로 재디자인했습니다.
- alerts-list/page-alerts-list 버튼 hover 시 텍스트가 흐려지는 현상을 줄이기 위해 부모 filter/opacity 기반 hover를 제거하고 배경/보더/그림자 중심 hover로 보정했습니다.
- AI 추천 카드의 상하 패딩을 줄이고 `+ 추가` 버튼을 우측 최상단 투명 탭형 버튼으로 재배치했습니다.
- 반응형 하단 빠른이동 메뉴 hover/active 상태를 경계선이 튀지 않는 부드러운 배경 강조 방식으로 조정했습니다.

## v10.3.8-hotfix-settings-button-align
- AI 추천 새로고침/로컬 설정 초기화 버튼의 아이콘+텍스트 중앙 정렬 보정.
- 라이트 퍼플 테마에서 설정 일반 버튼의 블럭형 배경/경계/그림자 질감 보강.
- `settings-list-card` 컨테이너 자체의 hover/active 이동 모션 제거.

## v10.3.8-hotfix.12 - Page Card Motion & Mobile Nav Glow

- `page-card`, `light-page-card`, `alerts-page-card` 컨테이너 hover/active 이동 모션을 제거했습니다.
- 하위 카드/버튼의 hover 모션은 유지했습니다.
- 모바일 하단 빠른이동 메뉴의 active/hover 배경을 경계가 보이지 않는 부드러운 그라데이션 glow 방식으로 보정했습니다.

## v10.3.8-hotfix.18 - Alert clear all + stock add motion review

- 알림 모달과 알림 탭에 `알림 모두제거` 버튼을 추가했습니다.
- 모두제거 시 현재 알림 기준에 잡힌 뉴스 ID를 숨김 처리하여 알림 목록과 배지가 즉시 0건으로 갱신됩니다.
- 알림 제거 버튼은 라이트/다크 테마 모두에서 흐려지지 않도록 별도 hover/active 스타일을 적용했습니다.
- `stock-add-card` 모션 구조를 점검했습니다.


## v10.3.8-hotfix-quote-css-import
- `layout.tsx`의 `import "./globals.css"` 타입 인식 오류 방지를 위해 `global.d.ts`에 CSS 모듈 선언을 추가했습니다.
- 관심 종목 요약의 하드코딩 가격/등락률을 제거했습니다.
- `/api/quotes`를 추가하여 국내 6자리 종목은 네이버 실시간 시세, 해외 심볼은 Yahoo quote를 통해 가격/등락률을 조회하도록 수정했습니다.
- 시세 조회 실패 시 임의 가격을 표시하지 않고 `시세 확인 중` 상태를 표시합니다.

## v10.3.8-hotfix-skim-sticky-watch-star
- 앱 표시명을 `NEWB/뉴브`에서 `SKIM`으로 변경했습니다.
- `layout.tsx` 메타데이터와 `manifest.json` PWA 표시명을 SKIM 기준으로 수정했습니다.
- 관심종목 요약 카드의 별표 제거 버튼을 우측 상단 absolute 위치로 이동해 긴 종목명과 겹치지 않도록 보정했습니다.
- 데스크톱 좌측 사이드바와 우측 필터 패널이 스크롤 시 화면에 맞춰 따라오도록 sticky 동작을 보강했습니다.
